diff --git a/components/sync/syncable/write_transaction.h b/components/sync/syncable/write_transaction.h
index 3d0a353ac09b1dcbfab0d5afc3345d78eb4ee8bd..8998b6b5e4edb17157f16fb5082ef70d53c93b52 100644
--- a/components/sync/syncable/write_transaction.h
+++ b/components/sync/syncable/write_transaction.h
@@ -44,6 +44,9 @@ class WriteTransaction : public BaseTransaction {
                    int64_t* transaction_version);
   ~WriteTransaction() override;
 
+  WriteTransaction(int, const base::Location& from_here, UserShare* share,
+      syncable::WriteTransaction* syncable_wr_tr);
+
   // Provide access to the syncable transaction from the API WriteNode.
   syncable::BaseTransaction* GetWrappedTrans() const override;
   syncable::WriteTransaction* GetWrappedWriteTrans() { return transaction_; }
@@ -63,6 +66,7 @@ class WriteTransaction : public BaseTransaction {
     transaction_ = trans;
   }
 
+  bool keep_ = false;
  private:
   void* operator new(size_t size);  // Transaction is meant for stack use only.
 
